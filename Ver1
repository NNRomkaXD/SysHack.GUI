local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

local Config = {
    BV_Enabled = false,
    Speed = 50,
    JumpPower = 50,
    Noclip = false,
    InfJump = false,
    Hitbox = false,
    HitboxSize = 10,
    Invis_Enabled = false,
    Invis_Transparency = 0.75,
    SeatTeleportPos = Vector3.new(9e9, 9e9, 9e9),
    TeleportEnabled = false,
    TeleportDistance = 10,
    DisableRagdoll = false,
    TouchFling = false,
    FlingPower = 9e9,
    TeleportTo = Vector3.new(0, 50, 0)
}

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "FrulesGui"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = game:GetService("CoreGui")

local Main = Instance.new("Frame", ScreenGui)
Main.Size = UDim2.new(0, 500, 0, 450)
Main.Position = UDim2.new(0.5, -250, 0.5, -225)
Main.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
Main.Active = true
Main.Draggable = true
Main.Name = "MainFrame"

local Corner = Instance.new("UICorner", Main)
Corner.CornerRadius = UDim.new(0, 8)

local Title = Instance.new("TextLabel", Main)
Title.Size = UDim2.new(1, 0, 0, 35)
Title.Position = UDim2.new(0, 0, 0, 0)
Title.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Title.Text = "Frules GUI"
Title.TextColor3 = Color3.new(1, 1, 1)
Title.Font = Enum.Font.SourceSansBold
Title.TextSize = 20
Instance.new("UICorner", Title).CornerRadius = UDim.new(0, 8)

local Tabs = Instance.new("Frame", Main)
Tabs.Size = UDim2.new(1, -20, 0, 30)
Tabs.Position = UDim2.new(0, 10, 0, 40)
Tabs.BackgroundTransparency = 1

local CharacterTab = Instance.new("TextButton", Tabs)
CharacterTab.Size = UDim2.new(0.33, -5, 1, 0)
CharacterTab.Position = UDim2.new(0, 0, 0, 0)
CharacterTab.BackgroundColor3 = Color3.fromRGB(65, 65, 65)
CharacterTab.Text = "CHARACTER"
CharacterTab.TextColor3 = Color3.new(1, 1, 1)
CharacterTab.Font = Enum.Font.SourceSansBold
CharacterTab.TextSize = 16
Instance.new("UICorner", CharacterTab).CornerRadius = UDim.new(0, 4)

local HitboxTab = Instance.new("TextButton", Tabs)
HitboxTab.Size = UDim2.new(0.33, -5, 1, 0)
HitboxTab.Position = UDim2.new(0.33, 5, 0, 0)
HitboxTab.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
HitboxTab.Text = "HITBOX"
HitboxTab.TextColor3 = Color3.new(1, 1, 1)
HitboxTab.Font = Enum.Font.SourceSansBold
HitboxTab.TextSize = 16
Instance.new("UICorner", HitboxTab).CornerRadius = UDim.new(0, 4)

local TeleportTab = Instance.new("TextButton", Tabs)
TeleportTab.Size = UDim2.new(0.33, -5, 1, 0)
TeleportTab.Position = UDim2.new(0.66, 10, 0, 0)
TeleportTab.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
TeleportTab.Text = "TELEPORT"
TeleportTab.TextColor3 = Color3.new(1, 1, 1)
TeleportTab.Font = Enum.Font.SourceSansBold
TeleportTab.TextSize = 16
Instance.new("UICorner", TeleportTab).CornerRadius = UDim.new(0, 4)

local Columns = Instance.new("Frame", Main)
Columns.Size = UDim2.new(1, -20, 1, -80)
Columns.Position = UDim2.new(0, 10, 0, 75)
Columns.BackgroundTransparency = 1

local CharacterFrame = Instance.new("Frame", Columns)
CharacterFrame.Size = UDim2.new(1, 0, 1, 0)
CharacterFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
CharacterFrame.Name = "CharacterFrame"
CharacterFrame.Visible = true
Instance.new("UICorner", CharacterFrame).CornerRadius = UDim.new(0, 6)

local CharacterScroll = Instance.new("ScrollingFrame", CharacterFrame)
CharacterScroll.Size = UDim2.new(1, -10, 1, -10)
CharacterScroll.Position = UDim2.new(0, 5, 0, 5)
CharacterScroll.BackgroundTransparency = 1
CharacterScroll.ScrollBarThickness = 4
CharacterScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
CharacterScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y

local CharacterList = Instance.new("UIListLayout", CharacterScroll)
CharacterList.Padding = UDim.new(0, 5)
CharacterList.SortOrder = Enum.SortOrder.LayoutOrder

local HitboxFrame = Instance.new("Frame", Columns)
HitboxFrame.Size = UDim2.new(1, 0, 1, 0)
HitboxFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
HitboxFrame.Name = "HitboxFrame"
HitboxFrame.Visible = false
Instance.new("UICorner", HitboxFrame).CornerRadius = UDim.new(0, 6)

local HitboxScroll = Instance.new("ScrollingFrame", HitboxFrame)
HitboxScroll.Size = UDim2.new(1, -10, 1, -10)
HitboxScroll.Position = UDim2.new(0, 5, 0, 5)
HitboxScroll.BackgroundTransparency = 1
HitboxScroll.ScrollBarThickness = 4
HitboxScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
HitboxScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y

local HitboxList = Instance.new("UIListLayout", HitboxScroll)
HitboxList.Padding = UDim.new(0, 5)
HitboxList.SortOrder = Enum.SortOrder.LayoutOrder

local TeleportFrame = Instance.new("Frame", Columns)
TeleportFrame.Size = UDim2.new(1, 0, 1, 0)
TeleportFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
TeleportFrame.Name = "TeleportFrame"
TeleportFrame.Visible = false
Instance.new("UICorner", TeleportFrame).CornerRadius = UDim.new(0, 6)

local TeleportScroll = Instance.new("ScrollingFrame", TeleportFrame)
TeleportScroll.Size = UDim2.new(1, -10, 1, -10)
TeleportScroll.Position = UDim2.new(0, 5, 0, 5)
TeleportScroll.BackgroundTransparency = 1
TeleportScroll.ScrollBarThickness = 4
TeleportScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
TeleportScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y

local TeleportList = Instance.new("UIListLayout", TeleportScroll)
TeleportList.Padding = UDim.new(0, 5)
TeleportList.SortOrder = Enum.SortOrder.LayoutOrder

CharacterTab.MouseButton1Click:Connect(function()
    CharacterFrame.Visible = true
    HitboxFrame.Visible = false
    TeleportFrame.Visible = false
    CharacterTab.BackgroundColor3 = Color3.fromRGB(65, 65, 65)
    HitboxTab.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    TeleportTab.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
end)

HitboxTab.MouseButton1Click:Connect(function()
    CharacterFrame.Visible = false
    HitboxFrame.Visible = true
    TeleportFrame.Visible = false
    CharacterTab.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    HitboxTab.BackgroundColor3 = Color3.fromRGB(65, 65, 65)
    TeleportTab.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
end)

TeleportTab.MouseButton1Click:Connect(function()
    CharacterFrame.Visible = false
    HitboxFrame.Visible = false
    TeleportFrame.Visible = true
    CharacterTab.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    HitboxTab.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    TeleportTab.BackgroundColor3 = Color3.fromRGB(65, 65, 65)
end)

local function createToggle(parent, name, prop, callback)
    local btn = Instance.new("TextButton", parent)
    btn.Size = UDim2.new(1, 0, 0, 30)
    btn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    btn.Text = ""
    btn.AutoButtonColor = false
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 4)
    
    local label = Instance.new("TextLabel", btn)
    label.Size = UDim2.new(0.7, -5, 1, 0)
    label.Position = UDim2.new(0, 8, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = name
    label.TextColor3 = Color3.new(1, 1, 1)
    label.Font = Enum.Font.SourceSans
    label.TextSize = 15
    label.TextXAlignment = Enum.TextXAlignment.Left
    
    local indicator = Instance.new("Frame", btn)
    indicator.Size = UDim2.new(0, 18, 0, 18)
    indicator.Position = UDim2.new(1, -25, 0.5, -9)
    indicator.BackgroundColor3 = Config[prop] and Color3.fromRGB(100, 200, 100) or Color3.fromRGB(60, 60, 60)
    Instance.new("UICorner", indicator).CornerRadius = UDim.new(1, 0)
    
    local function updateUI()
        indicator.BackgroundColor3 = Config[prop] and Color3.fromRGB(100, 200, 100) or Color3.fromRGB(60, 60, 60)
    end
    
    btn.MouseButton1Click:Connect(function()
        Config[prop] = not Config[prop]
        updateUI()
        if callback then callback(Config[prop]) end
    end)
    
    return btn, updateUI
end

local function createInput(parent, name, prop, suffix, isScientific)
    local frame = Instance.new("Frame", parent)
    frame.Size = UDim2.new(1, 0, 0, 30)
    frame.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 4)
    
    local label = Instance.new("TextLabel", frame)
    label.Size = UDim2.new(0.5, -5, 1, 0)
    label.Position = UDim2.new(0, 8, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = name
    label.TextColor3 = Color3.new(1, 1, 1)
    label.Font = Enum.Font.SourceSans
    label.TextSize = 15
    label.TextXAlignment = Enum.TextXAlignment.Left
    
    local box = Instance.new("TextBox", frame)
    box.Size = UDim2.new(0.5, -15, 1, -6)
    box.Position = UDim2.new(0.5, 5, 0, 3)
    box.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    box.TextColor3 = Color3.new(1, 1, 1)
    box.Font = Enum.Font.SourceSans
    box.TextSize = 14
    if isScientific then
        box.Text = string.format("%.0e", Config[prop])
    else
        box.Text = tostring(Config[prop]) .. (suffix and " " .. suffix or "")
    end
    box.ClearTextOnFocus = false
    Instance.new("UICorner", box).CornerRadius = UDim.new(0, 4)
    
    box.FocusLost:Connect(function()
        local text = box.Text:gsub("%s+", "")
        local num = tonumber(text)
        if num then
            Config[prop] = num
            if isScientific then
                box.Text = string.format("%.0e", Config[prop])
            else
                box.Text = tostring(num) .. (suffix and " " .. suffix or "")
            end
        else
            if isScientific then
                box.Text = string.format("%.0e", Config[prop])
            else
                box.Text = tostring(Config[prop]) .. (suffix and " " .. suffix or "")
            end
        end
    end)
end

local function createCoordinateInput(parent, name, prop)
    local frame = Instance.new("Frame", parent)
    frame.Size = UDim2.new(1, 0, 0, 30)
    frame.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 4)
    
    local label = Instance.new("TextLabel", frame)
    label.Size = UDim2.new(0.25, -5, 1, 0)
    label.Position = UDim2.new(0, 8, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = name
    label.TextColor3 = Color3.new(1, 1, 1)
    label.Font = Enum.Font.SourceSans
    label.TextSize = 15
    label.TextXAlignment = Enum.TextXAlignment.Left
    
    local box = Instance.new("TextBox", frame)
    box.Size = UDim2.new(0.55, -15, 1, -6)
    box.Position = UDim2.new(0.25, 5, 0, 3)
    box.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    box.TextColor3 = Color3.new(1, 1, 1)
    box.Font = Enum.Font.SourceSans
    box.TextSize = 14
    box.Text = string.format("%.1f %.1f %.1f", Config[prop].X, Config[prop].Y, Config[prop].Z)
    box.ClearTextOnFocus = false
    Instance.new("UICorner", box).CornerRadius = UDim.new(0, 4)
    
    box.FocusLost:Connect(function()
        local x, y, z = box.Text:match("([%-%d%.]+)%s+([%-%d%.]+)%s+([%-%d%.]+)")
        if x and y and z then
            Config[prop] = Vector3.new(tonumber(x) or 0, tonumber(y) or 0, tonumber(z) or 0)
            box.Text = string.format("%.1f %.1f %.1f", Config[prop].X, Config[prop].Y, Config[prop].Z)
        else
            box.Text = string.format("%.1f %.1f %.1f", Config[prop].X, Config[prop].Y, Config[prop].Z)
        end
    end)
    
    local teleportBtn = Instance.new("TextButton", frame)
    teleportBtn.Size = UDim2.new(0, 50, 0, 22)
    teleportBtn.Position = UDim2.new(1, -55, 0.5, -11)
    teleportBtn.BackgroundColor3 = Color3.fromRGB(65, 65, 65)
    teleportBtn.Text = "GO"
    teleportBtn.TextColor3 = Color3.new(1, 1, 1)
    teleportBtn.Font = Enum.Font.SourceSansBold
    teleportBtn.TextSize = 13
    Instance.new("UICorner", teleportBtn).CornerRadius = UDim.new(0, 4)
    
    teleportBtn.MouseEnter:Connect(function()
        teleportBtn.BackgroundColor3 = Color3.fromRGB(85, 85, 85)
    end)
    
    teleportBtn.MouseLeave:Connect(function()
        teleportBtn.BackgroundColor3 = Color3.fromRGB(65, 65, 65)
    end)
    
    teleportBtn.MouseButton1Click:Connect(function()
        teleportBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
        task.wait(0.1)
        teleportBtn.BackgroundColor3 = Color3.fromRGB(65, 65, 65)
        
        local char = LocalPlayer.Character
        local root = char and char:FindFirstChild("HumanoidRootPart")
        if root then
            root.CFrame = CFrame.new(Config[prop])
        end
    end)
end

local updateInvis = nil
local ragdollConnection = nil
local flingThread = nil

local function toggleRagdoll(state)
    local char = LocalPlayer.Character
    if not char then return end
    
    local hum = char:FindFirstChild("Humanoid")
    if not hum then return end
    
    if state then
        hum:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false)
        hum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false)
        
        if not ragdollConnection then
            ragdollConnection = hum.StateChanged:Connect(function(_, newState)
                if newState == Enum.HumanoidStateType.FallingDown or newState == Enum.HumanoidStateType.Ragdoll then
                    hum:ChangeState(Enum.HumanoidStateType.GettingUp)
                end
            end)
        end
    else
        hum:SetStateEnabled(Enum.HumanoidStateType.FallingDown, true)
        hum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, true)
        
        if ragdollConnection then
            ragdollConnection:Disconnect()
            ragdollConnection = nil
        end
    end
end

local function toggleTouchFling(state)
    if state then
        if not ReplicatedStorage:FindFirstChild("juisdfj0i32i0eidsuf0iok") then
            local detection = Instance.new("Decal")
            detection.Name = "juisdfj0i32i0eidsuf0iok"
            detection.Parent = ReplicatedStorage
        end
        
        local function fling()
            local lp = Players.LocalPlayer
            local c, hrp, vel, movel = nil, nil, nil, 0.1
        
            while Config.TouchFling do
                RunService.Heartbeat:Wait()
                c = lp.Character
                hrp = c and c:FindFirstChild("HumanoidRootPart")
        
                if hrp then
                    vel = hrp.Velocity
                    hrp.Velocity = vel * Config.FlingPower + Vector3.new(0, Config.FlingPower, 0)
                    RunService.RenderStepped:Wait()
                    hrp.Velocity = vel
                    RunService.Stepped:Wait()
                    hrp.Velocity = vel + Vector3.new(0, movel, 0)
                    movel = -movel
                end
            end
        end
        
        flingThread = coroutine.create(fling)
        coroutine.resume(flingThread)
    end
end

local function toggleInvis(state)
    local char = LocalPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    if state then
        for _, v in pairs(char:GetDescendants()) do 
            if v:IsA("BasePart") and v.Name ~= "HumanoidRootPart" then 
                v.Transparency = Config.Invis_Transparency 
            end 
        end
        local savedPos = hrp.CFrame
        char:MoveTo(Config.SeatTeleportPos)
        task.wait(0.1)
        local seat = Instance.new('Seat', workspace)
        seat.Name = "InvisChair"
        seat.Transparency = 1
        seat.Position = Config.SeatTeleportPos
        local weld = Instance.new("Weld", seat)
        weld.Part0 = seat
        weld.Part1 = char:FindFirstChild("Torso") or char:FindFirstChild("UpperTorso")
        task.wait()
        seat.CFrame = savedPos
    else
        for _, v in pairs(char:GetDescendants()) do 
            if v:IsA("BasePart") then 
                v.Transparency = 0 
            end 
        end
        if workspace:FindFirstChild("InvisChair") then 
            workspace.InvisChair:Destroy() 
        end
    end
    
    if updateInvis then
        updateInvis()
    end
end

createInput(CharacterScroll, "Walk Speed", "Speed", "studs/s")
createInput(CharacterScroll, "Jump Power", "JumpPower")
createToggle(CharacterScroll, "Body Velocity Mode", "BV_Enabled")
createToggle(CharacterScroll, "No Clip", "Noclip", function(state)
    if state then
        local char = LocalPlayer.Character
        if char then
            for _, v in pairs(char:GetDescendants()) do
                if v:IsA("BasePart") then
                    v.CanCollide = false
                end
            end
        end
    end
end)
createToggle(CharacterScroll, "Infinite Jump", "InfJump")
createToggle(CharacterScroll, "Disable Ragdoll", "DisableRagdoll", function(state)
    toggleRagdoll(state)
end)
createToggle(CharacterScroll, "Touch Fling", "TouchFling", function(state)
    toggleTouchFling(state)
end)
createInput(CharacterScroll, "Fling Power", "FlingPower", nil, true)

local invisToggle, invisUpdate = createToggle(CharacterScroll, "Invisible", "Invis_Enabled", function(state)
    toggleInvis(state)
end)
updateInvis = invisUpdate

createToggle(HitboxScroll, "Hitbox", "Hitbox")
createInput(HitboxScroll, "Hitbox Size", "HitboxSize", "studs")

local teleportConnection = nil
local TeleportToggle, updateTeleport = createToggle(TeleportScroll, "Teleport All Players", "TeleportEnabled", function(state)
    if state then
        teleportConnection = RunService.Heartbeat:Connect(function()
            local char = LocalPlayer.Character
            local root = char and char:FindFirstChild("HumanoidRootPart")
            if not root then return end

            local offsetPosition = root.Position + (root.CFrame.LookVector * Config.TeleportDistance)

            for _, player in ipairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Character then
                    local tRoot = player.Character:FindFirstChild("HumanoidRootPart")
                    if tRoot then
                        tRoot.CFrame = CFrame.new(offsetPosition)
                        tRoot.Position = offsetPosition
                    end
                end
            end
        end)
    else
        if teleportConnection then
            teleportConnection:Disconnect()
            teleportConnection = nil
        end
    end
end)

createInput(TeleportScroll, "Teleport Distance", "TeleportDistance", "studs")
createCoordinateInput(TeleportScroll, "Teleport To (X Y Z)", "TeleportTo")

local function resetHitboxes()
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("Head") then
            p.Character.Head.Size = Vector3.new(1.2, 1.2, 1.2)
            p.Character.Head.Transparency = 0
            p.Character.Head.CanCollide = true
        end
    end
end

RunService.Heartbeat:Connect(function(dt)
    local char = LocalPlayer.Character
    if not char then return end
    
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChild("Humanoid")
    if not hrp or not hum then return end

    local mBV = hrp:FindFirstChild("MoveBV")
    if not mBV then 
        mBV = Instance.new("BodyVelocity")
        mBV.Name = "MoveBV"
        mBV.Parent = hrp
        mBV.MaxForce = Vector3.zero
    end

    hum.JumpPower = Config.JumpPower

    if Config.BV_Enabled then
        hum.WalkSpeed = 16
        local dir = hum.MoveDirection
        if dir.Magnitude > 0 then
            mBV.MaxForce = Vector3.new(1e6, 0, 1e6)
            mBV.Velocity = dir * Config.Speed
        else 
            mBV.MaxForce = Vector3.zero
        end
    else
        mBV.MaxForce = Vector3.zero
        hum.WalkSpeed = Config.Speed
    end

    if Config.Noclip then
        for _, v in pairs(char:GetDescendants()) do
            if v:IsA("BasePart") then
                v.CanCollide = false
            end
        end
    else
        for _, v in pairs(char:GetDescendants()) do
            if v:IsA("BasePart") and not v.CanCollide then
                v.CanCollide = true
            end
        end
    end
end)

RunService.RenderStepped:Connect(function()
    if Config.Hitbox then
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("Head") then
                p.Character.Head.Size = Vector3.one * Config.HitboxSize
                p.Character.Head.Transparency = 0.5
                p.Character.Head.CanCollide = false
            end
        end
    else
        resetHitboxes()
    end
end)

UserInputService.JumpRequest:Connect(function()
    if Config.InfJump then
        local char = LocalPlayer.Character
        if char then
            local hum = char:FindFirstChild("Humanoid")
            if hum then
                hum:ChangeState(Enum.HumanoidStateType.Jumping)
            end
        end
    end
end)

UserInputService.InputBegan:Connect(function(i, g)
    if g then return end
    if i.KeyCode == Enum.KeyCode.Z then
        Config.Invis_Enabled = not Config.Invis_Enabled
        toggleInvis(Config.Invis_Enabled)
    elseif i.KeyCode == Enum.KeyCode.Insert then
        Main.Visible = not Main.Visible
    end
end)

Players.PlayerAdded:Connect(function(player)
    player.CharacterAdded:Connect(function(character)
        task.wait(0.5)
        if not Config.Hitbox then
            if character:FindFirstChild("Head") then
                character.Head.Size = Vector3.new(1.2, 1.2, 1.2)
                character.Head.Transparency = 0
                character.Head.CanCollide = true
            end
        end
        
        if Config.DisableRagdoll then
            task.wait(0.1)
            toggleRagdoll(true)
        end
    end)
end)
