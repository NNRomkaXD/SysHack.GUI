local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer

local Config = {
    BV_Enabled = false,
    Speed = 50,
    JumpPower = 50,
    Noclip = false,
    InfJump = false,
    Hitbox = false,
    HitboxSize = 10,
    Invis_Enabled = false,
    Invis_Transparency = 0.75,
    SeatTeleportPos = Vector3.new(-25, 400, 3500)
}

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "MoriendoGui"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = game:GetService("CoreGui")

local Main = Instance.new("Frame", ScreenGui)
Main.Size = UDim2.new(0, 500, 0, 400)
Main.Position = UDim2.new(0.5, -250, 0.5, -200)
Main.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
Main.Active = true
Main.Draggable = true
Main.Name = "MainFrame"

local Corner = Instance.new("UICorner", Main)
Corner.CornerRadius = UDim.new(0, 8)

local Title = Instance.new("TextLabel", Main)
Title.Size = UDim2.new(1, 0, 0, 35)
Title.Position = UDim2.new(0, 0, 0, 0)
Title.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Title.Text = "Moriendo Hub"
Title.TextColor3 = Color3.new(1, 1, 1)
Title.Font = Enum.Font.SourceSansBold
Title.TextSize = 20
Title.TextScaled = false
Instance.new("UICorner", Title).CornerRadius = UDim.new(0, 8)

local Columns = Instance.new("Frame", Main)
Columns.Size = UDim2.new(1, -20, 1, -50)
Columns.Position = UDim2.new(0, 10, 0, 40)
Columns.BackgroundTransparency = 1

local MovementFrame = Instance.new("Frame", Columns)
MovementFrame.Size = UDim2.new(0.5, -5, 1, 0)
MovementFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
MovementFrame.Name = "MovementFrame"
Instance.new("UICorner", MovementFrame).CornerRadius = UDim.new(0, 6)

local MovementHeader = Instance.new("TextLabel", MovementFrame)
MovementHeader.Size = UDim2.new(1, -10, 0, 30)
MovementHeader.Position = UDim2.new(0, 5, 0, 5)
MovementHeader.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
MovementHeader.Text = " MOVEMENT"
MovementHeader.TextColor3 = Color3.new(1, 1, 1)
MovementHeader.Font = Enum.Font.SourceSansBold
MovementHeader.TextSize = 18
MovementHeader.TextXAlignment = Enum.TextXAlignment.Left
Instance.new("UICorner", MovementHeader).CornerRadius = UDim.new(0, 4)

local MovementScroll = Instance.new("ScrollingFrame", MovementFrame)
MovementScroll.Size = UDim2.new(1, -10, 1, -45)
MovementScroll.Position = UDim2.new(0, 5, 0, 40)
MovementScroll.BackgroundTransparency = 1
MovementScroll.ScrollBarThickness = 4
MovementScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
MovementScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y

local MovementList = Instance.new("UIListLayout", MovementScroll)
MovementList.Padding = UDim.new(0, 5)
MovementList.SortOrder = Enum.SortOrder.LayoutOrder

local CombatFrame = Instance.new("Frame", Columns)
CombatFrame.Size = UDim2.new(0.5, -5, 1, 0)
CombatFrame.Position = UDim2.new(0.5, 5, 0, 0)
CombatFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
CombatFrame.Name = "CombatFrame"
Instance.new("UICorner", CombatFrame).CornerRadius = UDim.new(0, 6)

local CombatHeader = Instance.new("TextLabel", CombatFrame)
CombatHeader.Size = UDim2.new(1, -10, 0, 30)
CombatHeader.Position = UDim2.new(0, 5, 0, 5)
CombatHeader.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
CombatHeader.Text = " COMBAT"
CombatHeader.TextColor3 = Color3.new(1, 1, 1)
CombatHeader.Font = Enum.Font.SourceSansBold
CombatHeader.TextSize = 18
CombatHeader.TextXAlignment = Enum.TextXAlignment.Left
Instance.new("UICorner", CombatHeader).CornerRadius = UDim.new(0, 4)

local CombatScroll = Instance.new("ScrollingFrame", CombatFrame)
CombatScroll.Size = UDim2.new(1, -10, 1, -45)
CombatScroll.Position = UDim2.new(0, 5, 0, 40)
CombatScroll.BackgroundTransparency = 1
CombatScroll.ScrollBarThickness = 4
CombatScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
CombatScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y

local CombatList = Instance.new("UIListLayout", CombatScroll)
CombatList.Padding = UDim.new(0, 5)
CombatList.SortOrder = Enum.SortOrder.LayoutOrder

local VisualFrame = Instance.new("Frame", Columns)
VisualFrame.Size = UDim2.new(0.5, -5, 1, 0)
VisualFrame.Position = UDim2.new(0.5, 5, 0, 0)
VisualFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
VisualFrame.Visible = false
Instance.new("UICorner", VisualFrame).CornerRadius = UDim.new(0, 6)

local function createToggle(parent, name, prop, callback)
    local btn = Instance.new("TextButton", parent)
    btn.Size = UDim2.new(1, 0, 0, 30)
    btn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    btn.Text = ""
    btn.AutoButtonColor = false
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 4)
    
    local label = Instance.new("TextLabel", btn)
    label.Size = UDim2.new(0.7, -5, 1, 0)
    label.Position = UDim2.new(0, 8, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = name
    label.TextColor3 = Color3.new(1, 1, 1)
    label.Font = Enum.Font.SourceSans
    label.TextSize = 15
    label.TextXAlignment = Enum.TextXAlignment.Left
    
    local indicator = Instance.new("Frame", btn)
    indicator.Size = UDim2.new(0, 18, 0, 18)
    indicator.Position = UDim2.new(1, -25, 0.5, -9)
    indicator.BackgroundColor3 = Config[prop] and Color3.fromRGB(100, 200, 100) or Color3.fromRGB(60, 60, 60)
    Instance.new("UICorner", indicator).CornerRadius = UDim.new(1, 0)
    
    local function updateUI()
        indicator.BackgroundColor3 = Config[prop] and Color3.fromRGB(100, 200, 100) or Color3.fromRGB(60, 60, 60)
    end
    
    btn.MouseButton1Click:Connect(function()
        Config[prop] = not Config[prop]
        updateUI()
        if callback then callback(Config[prop]) end
    end)
    
    return btn, updateUI
end

local function createInput(parent, name, prop, suffix)
    local frame = Instance.new("Frame", parent)
    frame.Size = UDim2.new(1, 0, 0, 30)
    frame.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 4)
    
    local label = Instance.new("TextLabel", frame)
    label.Size = UDim2.new(0.5, -5, 1, 0)
    label.Position = UDim2.new(0, 8, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = name
    label.TextColor3 = Color3.new(1, 1, 1)
    label.Font = Enum.Font.SourceSans
    label.TextSize = 15
    label.TextXAlignment = Enum.TextXAlignment.Left
    
    local box = Instance.new("TextBox", frame)
    box.Size = UDim2.new(0.5, -15, 1, -6)
    box.Position = UDim2.new(0.5, 5, 0, 3)
    box.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    box.TextColor3 = Color3.new(1, 1, 1)
    box.Font = Enum.Font.SourceSans
    box.TextSize = 14
    box.Text = tostring(Config[prop]) .. (suffix and " " .. suffix or "")
    box.ClearTextOnFocus = false
    Instance.new("UICorner", box).CornerRadius = UDim.new(0, 4)
    
    box.FocusLost:Connect(function()
        local num = tonumber(box.Text:match("%d+%.?%d*"))
        if num then
            Config[prop] = num
            box.Text = tostring(num) .. (suffix and " " .. suffix or "")
        else
            box.Text = tostring(Config[prop]) .. (suffix and " " .. suffix or "")
        end
    end)
end

createInput(MovementScroll, "Walk Speed", "Speed", "studs/s")
createInput(MovementScroll, "Jump Power", "JumpPower")
createToggle(MovementScroll, "BV Mode", "BV_Enabled")
createToggle(MovementScroll, "Noclip", "Noclip")
createToggle(MovementScroll, "Inf Jump", "InfJump")

createToggle(CombatScroll, "Hitbox", "Hitbox")
createInput(CombatScroll, "Hitbox Size", "HitboxSize", "studs")

local InvisToggle, updateInvis = createToggle(MovementScroll, "Invisible [Z]", "Invis_Enabled", function(state)
    toggleInvis(state)
end)

local function toggleInvis(state)
    local char = LocalPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    if state then
        for _, v in pairs(char:GetDescendants()) do 
            if v:IsA("BasePart") and v.Name ~= "HumanoidRootPart" then 
                v.Transparency = Config.Invis_Transparency 
            end 
        end
        local savedPos = hrp.CFrame
        char:MoveTo(Config.SeatTeleportPos)
        task.wait(0.1)
        local seat = Instance.new('Seat', workspace)
        seat.Name = "InvisChair"
        seat.Transparency = 1
        seat.Position = Config.SeatTeleportPos
        local weld = Instance.new("Weld", seat)
        weld.Part0 = seat
        weld.Part1 = char:FindFirstChild("Torso") or char:FindFirstChild("UpperTorso")
        task.wait()
        seat.CFrame = savedPos
    else
        for _, v in pairs(char:GetDescendants()) do 
            if v:IsA("BasePart") then 
                v.Transparency = 0 
            end 
        end
        if workspace:FindFirstChild("InvisChair") then 
            workspace.InvisChair:Destroy() 
        end
    end
end

local function resetHitboxes()
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("Head") then
            p.Character.Head.Size = Vector3.new(1.2, 1.2, 1.2)
            p.Character.Head.Transparency = 0
            p.Character.Head.CanCollide = true
        end
    end
end

RunService.Heartbeat:Connect(function(dt)
    local char = LocalPlayer.Character
    if not char then return end
    
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChild("Humanoid")
    if not hrp or not hum then return end

    local mBV = hrp:FindFirstChild("MoveBV")
    if not mBV then 
        mBV = Instance.new("BodyVelocity")
        mBV.Name = "MoveBV"
        mBV.Parent = hrp
        mBV.MaxForce = Vector3.zero
    end

    hum.JumpPower = Config.JumpPower

    if Config.BV_Enabled then
        hum.WalkSpeed = 16
        local dir = hum.MoveDirection
        if dir.Magnitude > 0 then
            mBV.MaxForce = Vector3.new(1e6, 0, 1e6)
            mBV.Velocity = dir * Config.Speed
        else 
            mBV.MaxForce = Vector3.zero
        end
    else
        mBV.MaxForce = Vector3.zero
        hum.WalkSpeed = Config.Speed
    end

    if Config.Noclip then
        for _, v in pairs(char:GetDescendants()) do
            if v:IsA("BasePart") then
                v.CanCollide = false
            end
        end
    end
end)

RunService.RenderStepped:Connect(function()
    if Config.Hitbox then
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("Head") then
                p.Character.Head.Size = Vector3.one * Config.HitboxSize
                p.Character.Head.Transparency = 0.5
                p.Character.Head.CanCollide = false
            end
        end
    else
        resetHitboxes()
    end
end)

local jumpCooldown = false
UserInputService.JumpRequest:Connect(function()
    if Config.InfJump then
        local char = LocalPlayer.Character
        if char then
            local hum = char:FindFirstChild("Humanoid")
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hum and hrp and not jumpCooldown then
                jumpCooldown = true
                
                hum:ChangeState(Enum.HumanoidStateType.Jumping)
                hrp.Velocity = Vector3.new(hrp.Velocity.X, 50, hrp.Velocity.Z)
                
                task.wait(0.1)
                jumpCooldown = false
            end
        end
    end
end)

UserInputService.InputBegan:Connect(function(i, g)
    if g then return end
    if i.KeyCode == Enum.KeyCode.Z then
        Config.Invis_Enabled = not Config.Invis_Enabled
        toggleInvis(Config.Invis_Enabled)
        if updateInvis then updateInvis() end
    elseif i.KeyCode == Enum.KeyCode.Insert then
        Main.Visible = not Main.Visible
    end
end)

Players.PlayerAdded:Connect(function(player)
    player.CharacterAdded:Connect(function(character)
        task.wait(0.5)
        if not Config.Hitbox then
            if character:FindFirstChild("Head") then
                character.Head.Size = Vector3.new(1.2, 1.2, 1.2)
                character.Head.Transparency = 0
                character.Head.CanCollide = true
            end
        end
    end)
end)
